import Feed from './App';

function Login() {
  return (
    <div className="Login">
      <header className="Login-header">
          <button onClick={Feed}>
              LOGIN
          </button>
      </header>
    </div>
  );
}

export default Login;
